{
  "name": "klay-plus",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:core": "pnpm --filter @klay/core test",
    "dev": "pnpm --filter @klay/web dev"
  }
}
