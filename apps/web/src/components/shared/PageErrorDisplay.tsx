// Re-export ErrorDisplay — PageErrorDisplay was a subset with optional `code`.
// Kept for backward compatibility of existing imports.
export { ErrorDisplay as PageErrorDisplay } from "./ErrorDisplay";
